_schema-version: "3.2"
ID: Bitacora.bitacora
version: 0.0.1

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces

modules:
  - name: Bitacora.bitacora
    type: staticfile
    path: dist
    parameters:
      disk-quota: 256M
      memory: 256M